---
num_of_classes: 13

# Keras model paths (legacy — used by the existing inference pipeline)
model_path: ''
model_directory: 'data/13_classes'

board_algorithm: 'CPS'  # 'CPS' or 'Mine'

data_directory: 'testing_images'
result_directory: '.'

default: MobileNetV2

MobileNetV2:
  model_name: "MobileNetV2"
  img_size: 224

NASNetMobile:
  model_name: 'NASNetMobile'
  img_size: 224

InceptionResNetV2:
  model_name: 'InceptionResNetV2'
  img_size: 150

Xception:
  model_name: 'Xception'
  img_size: 299

# PyTorch model paths (new pipeline)
piece_model_path: 'checkpoints/pieces/best.ckpt'
orientation_model_path: 'checkpoints/orientation/best.ckpt'
piece_backbone: 'vit_small_patch16_224.dino'
orientation_backbone: 'mobilenetv3_small_100'

